# Plan: Promote bash_max_timeout_ms to TOML config

See `docs/superpowers/plans/2026-05-27-promote-bash-timeout-to-config-plan.md` for the canonical task list.

Execute the plan's tasks in order. The plan is the authoritative source — this PLAN.md is just a pointer.
